package pm.device.panel;

import pm.Button;

public enum PanelButton implements Button {
    PREVIOUS_CHAPTER,
    REWIND,
    STOP,
    PAUSE,
    PLAY,
    FAST_FORWARD,
    NEXT_CHAPTER,
    TOGGLE_MUTE;
}
