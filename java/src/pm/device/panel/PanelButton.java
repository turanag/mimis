package pm.device.panel;

import pm.Button;

public enum PanelButton implements Button {
    PREVIOUS,
    REWIND,
    STOP,
    PAUSE,
    PLAY,
    FORWARD,
    NEXT,
    VOLUME_DOWN,
    MUTE,
    VOLUME_UP,
    REPEAT,
    SHUFFLE;
}
