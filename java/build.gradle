subprojects {
	version = '0.1'

    apply plugin: 'java'
    apply plugin: 'eclipse'
	apply plugin: 'cpp-lib'
	apply plugin: 'maven'

    repositories {
       mavenCentral()
    }

    dependencies {
    }
}
